# 🚀 الاستضافة الدائمة لـ «حوار» — مرّة واحدة ولن تحتاج أحداً بعدها

هذا الملف يشرح كيف تنقل موقعك إلى **استضافة دائمة تعمل 24/7 بمفردها**، فتعمل تلقائياً وتُصلح أخطاءها بنفسها، ولا تحتاج إلى تشغيل أي شيء يدوياً.

**النتيجة:** رابط عام دائم مثل `https://hiwar-ai.glitch.me` يعمل ليل نهار بدون تدخل منك.

---

## الخطوة 0 — احصل على مفتاح Gemini مجاني (مرة واحدة، 5 دقائق)

الموقع يستخدم ذكاء Gemini المجاني من Google. ستحتاج مفتاحاً خاصاً بك:

1. افتح  <https://aistudio.google.com/apikey>
2. سجّل الدخول بحساب Google.
3. اضغط **«Create API key»** ثم انسخ المفتاح (يبدأ عادة بـ `AIza...`).
4. احتفظ به — ستضعه في الاستضافة في الخطوة التالية.

> ملاحظة أمان: لا تكتب المفتاح في الكود أبداً. سنضعه في **متغيرات البيئة (Env)** داخل الاستضافة، فيبقى مخفياً عن الزوار.

---

## الخيار الأول (موصى به): Glitch — الأسهل

مجاني، ويدعم Node.js مباشرة، ولا يحتاج بطاقة ائتمان.

1. أنشئ حساباً مجانياً على <https://glitch.com> (ببريدك أو حساب Google/GitHub).
2. اضغط **New project** ← **Import from GitHub**.
3. **قبل ذلك** يجب رفع المشروع إلى GitHub (مرة واحدة):
   - أنشئ حساباً مجانياً على <https://github.com>.
   - اضغط **New repository** وسمّه `hiwar`.
   - اضغط **uploading an existing file** واسحب كل ملفات المشروع (من مجلد `hiwar/`) وارفعه.
   - انسخ رابط المستودع مثل `https://github.com/اسمك/hiwar`.
4. عد إلى Glitch والصق رابط المستودع في **Import from GitHub** ← اضغط Import. سيثبّت Glitch المشروع ويشغّله تلقائياً.
5. افتح ملف **`.env`** في Glitch (من القائمة الجانبية Files) وأضف السطر:
   ```
   GEMINI_API_KEY=الصق_مفتاحك_هنا
   ```
   (املأه بمفتاحك من الخطوة 0، واحذف ملف `.env.example` أو اتركه — لا يضر).
6. انسخ رابط موقعك من أعلى Glitch: **Share → Live site** (مثل `https://اسم-مشروعك.glitch.me`). 🎉

### إبقاء الموقع مستيقظاً على Glitch (مهم)

Glitch المجاني «ينام» بعد ~5 دقائق بدون زيارات. الحل المجاني القياسي:

1. افتح <https://uptimerobot.com> وأنشئ حساباً مجانياً.
2. أضف **New monitor** ← نوع **HTTP(s)** ← الصق رابط موقعك ← الفاصل **كل 5 دقائق**.
3. سيُرسل UptimeRobot زيارة كل 5 دقائق، فيبقى الموقع مستيقظاً دائماً مجاناً.

> بديل مدفوع: «Boost» مشروع Glitch يمنع النوم نهائياً.

---

## الخيار الثاني: Render — مجاني و«أكثر احترافية»

1. ارفع المشروع إلى GitHub (كما في الخيار الأول).
2. أنشئ حساباً على <https://render.com> (تسجيل دخول بـ GitHub أسهل).
3. **New → Web Service** ← اختر مستودع `hiwar`.
4. الإعدادات:
   - **Runtime:** Node
   - **Build command:** `npm install`
   - **Start command:** `npm start`
   - **Instance type:** Free
5. في قسم **Environment** أضف:
   - `GEMINI_API_KEY` = مفتاحك من الخطوة 0
6. اضغط **Deploy**. بعد دقيقة أو دقيقتين يعطيك رابطاً دائماً مثل `https://hiwar.onrender.com`.

> Render المجاني ينام بعد ~15 دقيقة بدون زيارات ويستيقظ تلقائياً عند أول زيارة (تأخير بسيط في أول فتح). أضف مراقب UptimeRobot كل 5 دقائق ليبقى مستيقظاً دائماً.

---

## ماذا يعمل تلقائياً بعد الاستضافة؟ (نظام الإصلاح الذاتي)

بمجرد نشره، يعمل النظام **بدون أي تدخل**:

| المشكلة | ما يحدث تلقائياً |
|---|---|
| الخادم ينهار | الحارس `guard.js` يعيد تشغيله فوراً (مع مهلة تصاعدية) |
| الخادم يتوقف عن الاستجابة | فحص صحة كل 10 ثوانٍ يقتله ويعيد تشغيله |
| مفتاح/نموذج يفشل | تحويل تلقائي (Failover) إلى نموذج آخر متاح |
| حصة ممتلئة (429) | قاطع كهربائي (Circuit Breaker) + عدّ تنازلي + إعادة محاولة |
| شبكة الإنترنت ضعيفة | تراجع أسي مع اهتزاز + وضع طوارئ محلي يُجيب مؤقتاً |
| انقطاع الإنترنت عند الزائر | وضع الطوارئ المحلي يُجيب (وقت، عواصم، حساب، تحية) |
| ملفات قديمة في المتصفح | Service Worker + رقم إصدار يُحدّث تلقائياً |

كل هذا مثبّت ومُختبَر في المشروع الآن.

---

## ملاحظات مهمة حول بيانات المستخدمين

- حسابات الطالبات ومهامهن وعاداتهن تُحفظ تلقائياً في مجلد **`data/`** داخل المشروع، وتُزامَن عبر الأجهزة لأنها مخزّنة على الخادم (وليس في المتصفح).
- على Glitch/Render تبقى هذه البيانات محفوظة ما دام المشروع منشوراً (لا تُحذف عند النوم أو إعادة النشر العادية). في Render المجاني قد يُعاد ضبط القرص دورياً — إن حدث فسيُطلب من المستخدمين إعادة إنشاء حساباتهم فقط.
- مفتاح Gemini يُوضع في متغير البيئة `GEMINI_API_KEY` فقط، ويبقى مخفياً عن الزوار نهائياً.

## متى قد تحتاج المساعدة فعلاً (نادر جداً)؟

- إذا **غيّرت Google سعر/واجهة Gemini** في المستقبل البعيد.
- إذا أردت **إضافة مزوّد ذكاء جديد** أو **تغيير التصميم**.
- إذا **امتلأت حصة مفتاحك المجاني** — افتح إعدادات الموقع وبدّل إلى Groq/OpenRouter مجانيين، أو أنشئ مفتاح Gemini جديد.

أما تشغيل الخادم، والمراقبة، وإصلاح الأخطاء اليومية — فلا تحتاج أحداً بعد الآن.

---

## سؤال سريع: لماذا يجب رفع المشروع إلى GitHub أولاً؟

لأن Glitch وRender يستوردان المشاريع من GitHub بضغطة واحدة، ويعيدان النشر تلقائياً عند أي تعديل لاحق. إنها الطريقة الأسرع والأكثر موثوقية للنشر.
